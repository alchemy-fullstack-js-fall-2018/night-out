#!/bin/bash -e


TOKEN=$(curl -sq -H 'Content-Type: application/json' 'https://alchemy-night-out.herokuapp.com/api/auth/signin' -d '{"email":"ryan@mailinator.com","clearPassword":"123456"}' | jq -r '.token')
if [[ $TOKEN != 'null' ]]; then
    echo 'SUCCESS'
else 
    echo 'FAILED' 2>&1
fi